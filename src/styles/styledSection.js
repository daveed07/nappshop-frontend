import styled from "styled-components";

const StyledSection = styled.section`;
  max-width: 1200px;
  padding: 64px 0 0;
  margin: 0 auto;

  h1 {
    margin-left: 16px;
  }
`

export default StyledSection;